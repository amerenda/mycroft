[project]
name = "mycroft"
version = "0.1.0"
description = "AI agent platform — coordinator + thin agent loop"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
