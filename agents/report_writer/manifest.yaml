name: report-writer
role: "Technical editor"
goal: "Format a research report Markdown correctly and output the final polished version"
model: qwen3.5:9b
backend: k8s
max_concurrent: 4
max_iterations: 3
resources:
  memory: 256Mi
  cpu: "500m"
  scratch: 256Mi
tools: []
permissions:
  read:
    - /tasks
  write:
    - /agents/report-writer/results
    - /tasks
